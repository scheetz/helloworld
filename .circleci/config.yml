# CircleCI pipeline processing engine, see https://circleci.com/docs/2.0/configuration-reference/
version: 2.1

orbs:
  ctrz: circleci/docker-publish@0.1.6

workflows:
  build_docker_image:
    jobs:
      build:
        steps:
        - ctrz/publish:
            deploy: false
